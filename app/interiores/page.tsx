import { PageShell } from "../components";import { projects } from "../data";
export default function Interiores(){return <PageShell><section className="page-intro"><p className="eyebrow">03 · Interiores</p><div><h1>Interiores para<em>habitar.</em></h1><p>Criamos atmosferas que acolhem a rotina e revelam a identidade de quem vive em cada espaço.</p></div></section><section className="projects-list">{projects.slice(0,2).map((p,i)=><article className="project-card" key={p.slug}><span>0{i+1}</span><img src={p.image} style={{objectPosition:i?"right":"left"}} alt={p.title}/><div><small>Interiores · {p.place}</small><h2>{i?"Refúgio Essencial":"Apartamento Origem"}</h2><p>{p.text}</p><a className="text-link" href="#">Ver ambiente <span>↗</span></a></div></article>)}</section></PageShell>}
